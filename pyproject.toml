[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sentinel-surveillance"
version = "0.1.0"
description = "SENTINEL edge-AI surveillance system"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
