# SENTINEL — direct dependencies only.
# deepface & ultralytics pull their own transitive deps (retina-face, mtcnn,
# torch, torchvision, pandas, keras, gdown, …) — don't list those yourself.

ultralytics==8.2.103     # YOLOv8 + ByteTrack
deepface==0.0.93         # face recognition
tf-keras==2.17.0         # Keras-2 shim DeepFace needs on TF >= 2.16
numpy==1.26.4            # keep < 2.0 for Jetson / TF / Ultralytics compatibility
Flask==3.0.3
requests==2.32.3
python-dotenv==1.0.1

# ML runtime (opencv, tensorflow, torch):
#   • Jetson  → provided by JetPack / NVIDIA wheels — do NOT pip-install
#   • x86 dev → uncomment:
# opencv-python==4.10.0.84
# tensorflow==2.17.0
